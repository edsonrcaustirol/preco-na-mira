(()=>{function syncMenu(){const menu=document.getElementById('menu'),nav=document.getElementById('nav');if(!menu||!nav)return;const open=nav.classList.contains('open');menu.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-label',open?'Fechar menu':'Abrir menu')}function currentNav(){const clean=path=>{const value=(path||'/').replace(/\.html$/i,'').replace(/\/+$/,'');return value||'/'};const current=clean(location.pathname);document.querySelectorAll('#nav a[href]').forEach(a=>{let url;try{url=new URL(a.getAttribute('href'),location.href)}catch(_){return}if(url.origin!==location.origin)return;const active=a.classList.contains('active')||clean(url.pathname)===current;if(active)a.setAttribute('aria-current','page');else if(a.getAttribute('aria-current')==='page')a.removeAttribute('aria-current')})}function external(){document.querySelectorAll('a[target="_blank"]').forEach(a=>{const rel=new Set((a.getAttribute('rel')||'').split(/\s+/).filter(Boolean));rel.add('noopener');rel.add('noreferrer');a.setAttribute('rel',[...rel].join(' '))})}document.addEventListener('DOMContentLoaded',()=>{syncMenu();currentNav();external()})})();
