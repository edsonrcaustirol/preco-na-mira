(()=>{const S={powerbank:['🔋','Power banks'],carregador:['⚡','Carregadores USB-C / GaN'],hub:['🔗','Hubs USB-C'],'ram-notebook':['💾','Memória RAM para notebook']};let t=new URLSearchParams(location.search).get('tipo');if(!S[t])t='powerbank';let m=PNMGetAccessoryManager(t);const esc=(s='')=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));document.getElementById('filters').innerHTML=Object.entries(S).map(([k,v])=>`<button data-t="${k}" class="${k===t?'active':''}">${v[0]} ${v[1]}</button>`).join('');function render(){const A=PRODUTOS.filter(p=>p.tipoProduto==='acessorio'&&p.subtipoAcessorio===t);document.getElementById('title').textContent=S[t][0]+' '+S[t][1];document.getElementById('count').textContent=A.length+' '+PNMPlural(A.length,'produto','produtos');document.getElementById('compare').href='comparativo-acessorios.html?tipo='+t;document.getElementById('grid').innerHTML=A.map(p=>`<article class="accessory-card"><div><img src="${esc(p.imagem)}" loading="lazy" decoding="async" data-fallback-src="${esc(p.imagemFallback)}" alt="${esc(p.imagemAlt||p.nome)}"></div><small>${esc(p.marca)}</small><h3>${esc(p.nome)}</h3><p>${esc(p.resumo)}</p><button data-c="${p.id}" class="compare-toggle ${m.has(p.id)?'selected':''}">${m.has(p.id)?'✓ SELECIONADO':'⚖️ COMPARAR'}</button><div class="actions"><a class="btn btn-dark" href="produto-${p.id}.html">ANALISAR</a><a class="btn btn-outline" href="${p.linkAfiliado}" target="_blank" rel="sponsored nofollow noopener noreferrer">OFERTA</a></div></article>`).join('');document.querySelectorAll('[data-c]').forEach(b=>b.onclick=()=>{const r=m.toggle(b.dataset.c);if(r==='limit')alert('Até 4 itens.');render()})}document.getElementById('filters').onclick=e=>{const b=e.target.closest('[data-t]');if(!b)return;t=b.dataset.t;m=PNMGetAccessoryManager(t);history.replaceState(null,'','?tipo='+t);document.querySelectorAll('[data-t]').forEach(x=>x.classList.toggle('active',x===b));render()};render()})();
