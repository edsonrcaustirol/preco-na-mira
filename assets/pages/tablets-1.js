const TYPE='tablet', MAN=PNMTabletCompare; const grid=document.getElementById('produtos'),vazio=document.getElementById('vazio'),contador=document.getElementById('contador'),busca=document.getElementById('busca');let filtro='todos',tier='todos';
function esc(s=''){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function tierOK(p){if(tier==='todos')return true;const f=(p.faixa||'').toLowerCase();return tier==='Custo-benefício'?f.includes('custo'):tier==='Intermediário'?f.includes('intermedi'):tier==='Topo de linha'?f.includes('topo'):true}
function render(){const q=busca.value.trim().toLowerCase();const lista=PRODUTOS.filter(p=>p.tipoProduto===TYPE).filter(p=>{const ok=filtro==='todos'||(p.marca||'').toLowerCase()===filtro;const texto=[p.nome,p.marca,p.resumo,p.chamada,p.faixa,...(p.chips||[]),...Object.values(p.especificacoes||{})].join(' ').toLowerCase();return ok&&tierOK(p)&&(!q||texto.includes(q))});grid.innerHTML=lista.map(p=>{const sel=MAN.has(p.id);return `<article class="product-card ${sel?'is-selected':''}"><div class="product-media">${p.selo?`<span class="tag">${esc(p.selo)}</span>`:''}<img src="${esc(p.imagem)}" loading="lazy" decoding="async" alt="${esc(p.imagemAlt||p.nome)}"></div><div class="product-copy"><div class="eyeline">${esc(p.marca)} • ${esc(p.categoria)}</div><h3>${esc(p.nome)}</h3><p>${esc(p.resumo)}</p><div class="spec-pills">${(p.chips||[]).slice(0,5).map(x=>`<span>${esc(x)}</span>`).join('')}</div><button class="compare-toggle ${sel?'selected':''}" type="button" data-compare="${esc(p.id)}">${sel?'✓ SELECIONADO PARA COMPARAR':'⚖️ COMPARAR ESTE PRODUTO'}</button><div class="actions"><a class="btn btn-dark" href="produto-${encodeURIComponent(p.id)}.html">VER ANÁLISE →</a><a class="btn btn-outline" href="${esc(p.linkAfiliado)}" target="_blank" rel="sponsored nofollow noopener noreferrer">VER OFERTA</a></div></div></article>`}).join('');vazio.style.display=lista.length?'none':'block';contador.textContent=`${lista.length} produto${lista.length===1?'':'s'} encontrado${lista.length===1?'':'s'}.`;bind();updateTray()}
function bind(){document.querySelectorAll('[data-compare]').forEach(btn=>btn.onclick=()=>{const r=MAN.toggle(btn.dataset.compare);if(r==='limit'){alert('Você pode comparar até 4 produtos por vez.');return}render()})}
function updateTray(){const ids=MAN.get(),items=ids.map(id=>PRODUTOS.find(p=>p.id===id)).filter(Boolean);const tray=document.getElementById('compareTray');tray.classList.toggle('show',items.length>0);document.getElementById('trayCount').textContent=`${items.length} de 4 escolhidos`;document.getElementById('trayItems').innerHTML=items.map(p=>`<div class="compare-chip"><span>${esc(p.nome)}</span><button data-remove="${esc(p.id)}">×</button></div>`).join('');document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{MAN.remove(b.dataset.remove);render()});document.getElementById('trayGo').classList.toggle('disabled',items.length<2)}
function buscar(){render();document.getElementById('achados').scrollIntoView({behavior:'smooth'})} document.querySelectorAll('.brand-filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.brand-filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');filtro=b.dataset.brand;render()});document.querySelectorAll('.tier-filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tier-filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');tier=b.dataset.tier;render()});document.getElementById('trayClear').onclick=()=>{MAN.clear();render()};render();
