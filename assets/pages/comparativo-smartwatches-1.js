const TYPE='smartwatch', MAN=PNMWearCompare, FIELDS=["Compatibilidade", "Tela", "Bateria", "GPS", "Chamadas", "Resistência", "LTE", "Destaque"]; function esc(s=''){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))} const all=PRODUTOS.filter(p=>p.tipoProduto===TYPE); let ids=MAN.get().filter(id=>all.some(p=>p.id===id)); if(ids.length<2) ids=all.slice(0,3).map(p=>p.id); MAN.set(ids);
function opts(selected){return all.map(p=>`<option value="${esc(p.id)}" ${p.id===selected?'selected':''}>${esc(p.marca)} — ${esc(p.nome)}</option>`).join('')}
function render(){ids=MAN.get().filter(id=>all.some(p=>p.id===id)); const ps=ids.map(id=>all.find(p=>p.id===id)).filter(Boolean); document.getElementById('builder').innerHTML=`<div class="section-head"><div><h2>Escolha os modelos</h2><p>Troque qualquer item abaixo. Até 4 por comparação.</p></div><button class="btn btn-outline" id="add">+ ADICIONAR MODELO</button></div><div class="compare-select-grid">${ps.map((p,i)=>`<div class="compare-select-card"><label>Modelo ${i+1}</label><select data-slot="${i}">${opts(p.id)}</select><button data-del="${i}">Remover</button></div>`).join('')}</div>`; document.getElementById('add').disabled=ps.length>=4; document.getElementById('add').onclick=()=>{if(ps.length>=4)return;const add=all.find(p=>!ids.includes(p.id));if(add){ids.push(add.id);MAN.set(ids);render()}};document.querySelectorAll('[data-slot]').forEach(s=>s.onchange=()=>{const i=+s.dataset.slot;const next=s.value;if(ids.includes(next)&&ids[i]!==next){alert('Esse modelo já está na comparação.');render();return}ids[i]=next;MAN.set(ids);render()});document.querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>{if(ids.length<=2){alert('Mantenha pelo menos 2 modelos para comparar.');return}ids.splice(+b.dataset.del,1);MAN.set(ids);render()});
const summaries=ps.map(p=>`<article class="decision-card"><div class="emoji">${TYPE==='tv'?'📺':TYPE==='smartwatch'?'⌚':'🎬'}</div><h3>${esc(p.nome)}</h3><p>${esc(p.chamada||p.resumo)}</p><a href="produto-${encodeURIComponent(p.id)}">Ver análise →</a></article>`).join('');document.getElementById('summary').innerHTML=`<div class="section-head"><div><h2>Em português claro</h2><p>O papel de cada opção antes da ficha técnica.</p></div></div><div class="decision-grid">${summaries}</div>`;
let rows=FIELDS.map(f=>`<tr><td>${esc(f)}</td>${ps.map(p=>`<td>${esc(p.especificacoes?.[f]??'—')}</td>`).join('')}</tr>`).join('');document.getElementById('tableWrap').innerHTML=`<table class="comparison-table"><thead><tr><th>Característica</th>${ps.map(p=>`<th><span class="model">${esc(p.nome)}</span></th>`).join('')}</tr></thead><tbody>${rows}<tr><td>Nossa leitura</td>${ps.map(p=>`<td>${esc(p.veredito)}</td>`).join('')}</tr><tr><td>Oferta</td>${ps.map(p=>`<td><a class="btn btn-dark" href="${esc(p.linkAfiliado)}" target="_blank" rel="sponsored nofollow noopener noreferrer">VER PREÇO →</a></td>`).join('')}</tr></tbody></table>`}
render();
