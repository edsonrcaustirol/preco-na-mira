(()=>{const S={powerbank:'Power banks',carregador:'Carregadores USB-C / GaN',hub:'Hubs USB-C','ram-notebook':'Memória RAM para notebook'},CR={powerbank:['Capacidade','Potência','Portas'],carregador:['Potência','Portas','Tecnologia'],hub:['Vídeo','Potência','Portas','Ethernet'],'ram-notebook':['Capacidade','Tipo','Frequência','Latência']};let t=new URLSearchParams(location.search).get('tipo');if(!S[t])t='powerbank';const sel=document.getElementById('type');sel.innerHTML=Object.entries(S).map(([k,v])=>`<option value="${k}">${v}</option>`).join('');sel.value=t;let m=PNMGetAccessoryManager(t);const esc=(s='')=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const val=(p,k)=>{const o=p.especificacoes||{},x=Object.keys(o).find(z=>z.toLowerCase().includes(k.toLowerCase()));return x?o[x]:'—'};function render(){const A=PRODUTOS.filter(p=>p.tipoProduto==='acessorio'&&p.subtipoAcessorio===t),C=m.get().map(id=>PRODUTOS.find(p=>p.id===id)).filter(Boolean);document.getElementById('choices').innerHTML=A.map(p=>`<button class="compare-choice ${m.has(p.id)?'active':''}" data-id="${p.id}"><img src="${esc(p.imagem)}" loading="lazy" decoding="async" alt="${esc(p.imagemAlt||p.nome)}"><span><b>${esc(p.nome)}</b><small>${esc(p.marca)}</small></span><i>${m.has(p.id)?'✓':'＋'}</i></button>`).join('');document.querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>{const r=m.toggle(b.dataset.id);if(r==='limit')alert('Até 4 itens.');render()});document.getElementById('table').innerHTML=C.length?`<div class="compare-row head"><b>Produto</b>${C.map(p=>`<span><img src="${esc(p.imagem)}" loading="lazy" decoding="async" alt="${esc(p.imagemAlt||p.nome)}"><strong>${esc(p.nome)}</strong><a href="${p.linkAfiliado}" target="_blank" rel="sponsored nofollow noopener noreferrer">OFERTA ↗</a></span>`).join('')}</div>${(CR[t]||[]).map(k=>`<div class="compare-row"><b>${k}</b>${C.map(p=>`<span>${esc(val(p,k))}</span>`).join('')}</div>`).join('')}`:'<div class="notice">Selecione itens abaixo para comparar.</div>'}sel.onchange=()=>{t=sel.value;m=PNMGetAccessoryManager(t);history.replaceState(null,'','?tipo='+t);render()};render()})();
