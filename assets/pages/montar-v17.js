(()=>{
 const $=s=>document.querySelector(s), esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
 const order=['cozinha','sala-cinema','banheiro','lavanderia','home-office','setup-gamer','compacto','casa-inteligente','casa'];
 function projectCard(p){const env=PNMProjects.environments[p.type]||PNMProjects.environments.cozinha,pct=PNMProjects.completion(p),issues=PNMProjects.diagnostics(p).filter(x=>['critical','warning'].includes(x.level)).length;return `<a class="pj-project-card" href="projeto.html?projeto=${encodeURIComponent(p.id)}"><span class="pj-card-no">${esc(env.label).toUpperCase()}</span><h3>${esc(p.name)}</h3><p>${esc(env.description)}</p><div class="pj-card-foot"><span>${issues?issues+' pendência'+(issues===1?'':'s'):'sem pendências críticas'}</span><b>${pct}%</b></div><div class="pj-progress-line"><i style="width:${pct}%"></i></div></a>`}
 function render(){const list=PNMProjects.list();$('#projectList').innerHTML=list.length?`<div class="pj-project-grid">${list.slice(0,6).map(projectCard).join('')}</div>`:`<div class="pj-empty"><strong>Nenhum projeto criado ainda.</strong>Escolha um ambiente abaixo. O primeiro projeto leva menos de um minuto para começar.</div>`;$('#templates').innerHTML=order.slice(0,6).map((k,i)=>{const e=PNMProjects.environments[k];return `<button type="button" class="pj-template-card" data-new="${k}"><span class="pj-card-no">${String(i+1).padStart(2,'0')}</span><h3>${esc(e.label)}</h3><p>${esc(e.description)}</p><span class="pj-card-foot"><span>CRIAR PROJETO</span><b>→</b></span></button>`}).join('')}
 document.addEventListener('click',e=>{const b=e.target.closest('[data-new]');if(!b)return;const p=PNMProjects.create(b.dataset.new);location.href=`projeto.html?projeto=${encodeURIComponent(p.id)}`});
 render();
})();