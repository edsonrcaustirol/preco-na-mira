const A=PNM_AMBIENTES.filter(a=>a.universo==='tecnologia');document.getElementById('count').textContent=PNMProductsForUniverse('tecnologia').length+' produtos conectados';document.getElementById('tech').innerHTML=A.map(a=>`<a class="environment-card" href="${a.href}"><div class="env-icon">${a.icon}</div><span class="kicker">${PNMProductsForEnvironment(a.id).length} ${PNMPlural(PNMProductsForEnvironment(a.id).length,'produto relacionado','produtos relacionados')}</span><h3>${a.nome}</h3><p>${a.descricao}</p><strong>ENTRAR →</strong></a>`).join('');
