(()=>{
const esc=(s='')=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
const nice=s=>String(s||'').replace(/[-_]/g,' ').replace(/\b\w/g,c=>c.toUpperCase());
const input=document.getElementById('catalogSearch'),universe=document.getElementById('catalogUniverse'),type=document.getElementById('catalogType'),brand=document.getElementById('catalogBrand'),tier=document.getElementById('catalogTier'),sort=document.getElementById('catalogSort'),grid=document.getElementById('catalogGrid'),count=document.getElementById('catalogCount'),more=document.getElementById('catalogMore');
let limit=48;
const toolbar=document.querySelector('.pnm-catalog-toolbar');
if(toolbar){const shortcut=document.createElement('div');shortcut.className='pnm-catalog-meta';shortcut.innerHTML='<strong>LINHA DEWALT</strong><span>Ferramentas da marca agora têm uma vitrine própria, com organização por família e destaque para séries especiais.</span><a class="pnm-catalog-more" href="dewalt.html" style="text-decoration:none;white-space:nowrap">ABRIR DEWALT →</a>';toolbar.parentNode.insertBefore(shortcut,toolbar)}
const universeDefs=[['casa','Casa'],['gamer','Gamer'],['tecnologia','Tecnologia']];
universe.innerHTML+=[...universeDefs].map(([v,t])=>`<option value="${v}">${t}</option>`).join('');
const types=[...new Set(PRODUTOS.map(p=>p.tipoProduto||p.categoriaId||p.categoria).filter(Boolean))].sort((a,b)=>String(a).localeCompare(String(b),'pt-BR'));
const brands=[...new Set(PRODUTOS.map(p=>p.marca).filter(Boolean))].sort((a,b)=>String(a).localeCompare(String(b),'pt-BR'));
const tiers=[...new Set(PRODUTOS.map(p=>p.faixa||p.selo).filter(Boolean))].sort((a,b)=>String(a).localeCompare(String(b),'pt-BR'));
type.innerHTML+=types.map(x=>`<option value="${esc(x)}">${esc(nice(x))}</option>`).join('');
brand.innerHTML+=brands.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join('');
tier.innerHTML+=tiers.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join('');
const productUniverses=p=>{try{return (PNMExperienceMeta(p).universos||[])}catch(e){const t=p.tipoProduto;return ['casa','cozinha','lavanderia','obra','instalacao','acabamento'].includes(t)?['casa']:['gamer'].includes(t)?['gamer']:['tecnologia']}};
function card(p){const img=p.imagem||p.imagemFallback||'assets/product-placeholder.svg';const fallback=p.imagemFallback||'assets/product-placeholder.svg';return `<article class="pnm-product-card"><a class="pnm-product-photo" href="produto-${encodeURIComponent(p.id)}.html"><img src="${esc(img)}" alt="${esc(p.imagemAlt||p.nome)}" loading="lazy" decoding="async" data-fallback-src="${esc(fallback)}"></a><div class="pnm-product-copy"><div class="pnm-product-top"><span>${esc(p.marca||'Produto')}</span>${p.destaque?'<b>DESTAQUE</b>':''}</div><h2>${esc(p.nome)}</h2><p>${esc(p.resumo||p.chamada||p.categoria||'Veja detalhes, contexto e oferta.')}</p><div class="pnm-product-actions"><a href="produto-${encodeURIComponent(p.id)}.html">ANALISAR</a><a class="offer" href="${esc(p.linkAfiliado||'#')}" target="_blank" rel="sponsored nofollow noopener noreferrer">VER OFERTA</a></div></div></article>`}
function render(reset=false){if(reset)limit=48;const q=norm(input.value.trim());let arr=PRODUTOS.filter(p=>(!universe.value||productUniverses(p).includes(universe.value))&&(!type.value||(p.tipoProduto||p.categoriaId||p.categoria)===type.value)&&(!brand.value||p.marca===brand.value)&&(!tier.value||(p.faixa||p.selo)===tier.value)&&(!q||norm([p.nome,p.marca,p.categoria,p.tipoProduto,p.subtipo,p.resumo,p.chamada,...(p.chips||[])].join(' ')).includes(q)));if(sort.value==='az')arr.sort((a,b)=>a.nome.localeCompare(b.nome,'pt-BR'));else if(sort.value==='brand')arr.sort((a,b)=>(a.marca||'').localeCompare(b.marca||'','pt-BR')||a.nome.localeCompare(b.nome,'pt-BR'));else arr.sort((a,b)=>(b.destaque?1:0)-(a.destaque?1:0)||a.nome.localeCompare(b.nome,'pt-BR'));count.textContent=`${arr.length} ${arr.length===1?'produto':'produtos'} de ${PRODUTOS.length}`;grid.innerHTML=arr.slice(0,limit).map(card).join('')||'<div class="pnm-empty"><b>Nenhum produto encontrado.</b><span>Tente outra palavra ou remova algum filtro.</span></div>';more.hidden=arr.length<=limit;more.onclick=()=>{limit+=48;render(false)}}
[input,universe,type,brand,tier,sort].forEach(el=>el.addEventListener(el===input?'input':'change',()=>render(true)));
render(true)
})();
