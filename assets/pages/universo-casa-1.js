(()=>{const rooms=PNM_AMBIENTES.filter(a=>a.universo==='casa');document.getElementById('count').textContent=PNMProductsForUniverse('casa').length+' produtos conectados';document.getElementById('rooms').innerHTML=rooms.map(a=>{const active=a.status==='ativo',n=active?PNMProductsForEnvironment(a.id).length:0,content=`<span class="room-icon">${a.icon}</span><div><small>${active?n+' '+PNMPlural(n,'produto relacionado','produtos relacionados'):a.status==='preparando'?'PREPARANDO':'PLANEJADO'}</small><h3>${a.nome}</h3><p>${a.descricao}</p></div><strong>${active?'ABRIR →':'EM BREVE'}</strong>`;return active?`<a class="room-card active" href="${a.href}">${content}</a>`:`<article class="room-card future">${content}</article>`}).join('')})();
