(()=>{const esc=(s='')=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();let filter='';const grid=document.getElementById('offerGrid'),tabs=document.getElementById('offerTabs');function bucket(p){const t=norm([p.tipoProduto,p.categoriaId,p.categoria,p.subtipo,p.subtipoCozinha].join(' '));if(/gamer|pc|gpu|processador|monitor|mouse|teclado|memoria|placa/.test(t))return'gamer';if(/casa|obra|instal|acabamento|banheiro|hidraul/.test(t))return'casa';if(/cozinha|airfryer|cafeteira|geladeira|fogao|forno|panela|lava-loucas/.test(t))return'cozinha';return'tecnologia'}function card(p){const img=p.imagem||p.imagemFallback||'assets/product-placeholder.svg';const fallback=p.imagemFallback||'assets/product-placeholder.svg';return `<article class="pnm-offer-card"><div class="pnm-offer-image"><img src="${esc(img)}" alt="${esc(p.imagemAlt||p.nome)}" loading="lazy" data-fallback-src="${esc(fallback)}"><span>${esc(p.selo||'NO RADAR')}</span></div><div class="pnm-offer-copy"><small>${esc(p.marca||p.categoria||'Produto')}</small><h3>${esc(p.nome)}</h3><p>${esc(p.chamada||p.resumo||'Veja a análise e confira a condição da oferta.')}</p><div><a href="produto-${encodeURIComponent(p.id)}.html">ANALISAR</a><a class="hot" href="${esc(p.linkAfiliado||'#')}" target="_blank" rel="sponsored nofollow noopener noreferrer">VER OFERTA</a></div></div></article>`}function render(){let arr=PRODUTOS.filter(p=>p.linkAfiliado&&(p.destaque||p.faixa||p.selo));if(filter)arr=arr.filter(p=>bucket(p)===filter);arr.sort((a,b)=>(b.destaque?1:0)-(a.destaque?1:0)||String(a.nome).localeCompare(String(b.nome),'pt-BR'));grid.innerHTML=arr.slice(0,24).map(card).join('')}tabs.addEventListener('click',e=>{const b=e.target.closest('[data-filter]');if(!b)return;filter=b.dataset.filter;tabs.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));render()});render()})();
